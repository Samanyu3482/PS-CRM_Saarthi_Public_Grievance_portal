services:
  - type: web
    name: indiainnovate-backend
    env: python
    region: singapore # Optional: adjust based on preference
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
